# bank43

Простой сайт на Elm для портфолио: заказ чашки с выбором цвета и принта.

## Установка и запуск

```bash
npm install
npm run build
```

Откройте `index.html` в браузере или запустите `npm start` и перейдите на `http://localhost:8000`.
